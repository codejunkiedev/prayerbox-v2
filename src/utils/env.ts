/**
 * Environment variable indicating if the app is running in production mode
 */
export const isProd = import.meta.env.PROD;

/**
 * Environment variable indicating if the app is running in development mode
 */
export const isDev = import.meta.env.DEV;
